name: cashier_pro
description: "CASHIER PRO - نظام كاشير وإدارة مبيعات"
publish_to: 'none'

version: 3.0.0+3

environment:
  sdk: '>=3.12.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # حفظ البيانات محليًا
  shared_preferences: ^2.5.3

  # فتح واتساب والاتصال المباشر
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true